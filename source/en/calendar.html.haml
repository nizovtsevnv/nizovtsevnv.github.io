---
blog: en
pageable: true
---

%h2
  - case page_type
    - when 'day'
      = Date.new(year, month, day).strftime('%b %e %Y')
    - when 'month'
      = Date.new(year, month, 1).strftime('%b %Y')
    - when 'year'
      = year

= partial 'partials/articles', locals: {prefix: 'en'}
